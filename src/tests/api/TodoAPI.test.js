import React from 'react';
import { shallow } from 'enzyme';

import TodoAPI from '../../utils/TodoAPI';

describe('TodoAPI', () => {

  it('should exist', () => {
    expect(TodoAPI).toBeDefined();
  });
});
